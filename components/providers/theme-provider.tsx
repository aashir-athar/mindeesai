"use client";
/**
 * Theme provider — currently dark-only. Stub kept so future light-mode work
 * has a single seam to pull through.
 */
export function ThemeProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
