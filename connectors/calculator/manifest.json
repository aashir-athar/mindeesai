{
  "name": "calculator",
  "version": "1.0.0",
  "description": "Evaluate a mathematical expression deterministically (no LLM). Supports +, -, *, /, %, **, parentheses, and named constants pi/e. Use this whenever you'd otherwise risk an arithmetic mistake.",
  "parameters": {
    "type": "object",
    "properties": {
      "expression": { "type": "string", "description": "Math expression, e.g. '(2.5 + 3.1) * 12'" }
    },
    "required": ["expression"]
  },
  "permissions": [],
  "author": "aashir-athar"
}
